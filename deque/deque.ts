const deque: number[] = [];

deque.push(2);
deque.push(3);
console.log("deque", deque);

deque.unshift(4);
console.log("deque", deque);

deque.shift();
console.log("deque", deque);

deque.pop();
console.log("deque", deque);

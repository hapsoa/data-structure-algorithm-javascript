class Node {
  constructor(data) {
    this.data = data;
    this.links = [];
  }

  setLink(node) {
    this.links.push(node);
  }
}

class Graph {
  constructor(edges) {

  }
}

function solution(n, edge) {
  let answer = 0;
  return answer;
}
